# Write a program to test if any of the elements pf a given array are non zero
import numpy as np
x= np.array ({1,0,0,0})
print(" Original array :")
print(x)
print(np.array(x))
x=np.array([0,0,0,0])
print(np.array(x))
