#Function to check the prime numbers between 1 to 100
def find_prime():
  for num in range(2,101):
    for i in range(2,num):
      if num %i == 0:
        break
    else:
      print(num)
find_prime()
